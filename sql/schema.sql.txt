CREATE TABLE IF NOT EXISTS predictions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    animal_type TEXT NOT NULL,
    breed TEXT,
    age REAL,
    gender TEXT,
    location TEXT,
    rescue_status TEXT,
    predicted_health_status TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);